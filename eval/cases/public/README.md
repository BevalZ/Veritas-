# Optional Public-Paper Evaluations

Public-paper evaluations are intentionally separate from default synthetic
replay tests. They may require third-party provider calls and must be run only
in explicit record mode with configured services.

Record outputs must be written outside default replay fixtures until reviewed.
